[project]
name = "unwrap-indexer"
authors = ["Unwrap Technologies"]
description = "Advanced asset tracking and verification infrastructure"
telemetry = false
requirements = []
[contracts.asset-tracking]
path = "contracts/asset-tracking.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
